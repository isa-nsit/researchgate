<div class="row">
	<div class="col-md-6 col-md-offset-3">
		<div class="panel panel-primary">
			<div class="panel-heading">New Project</div>
			<div class="panel-body">
				<%= form_for @project, url:projects_create_path(@project) do |f| %>
				  <% if @project.errors.any? %>
      				<h2><%= pluralize(@project.errors.count, "error") %> prohibited this project from being saved:</h2>

      			<ul>
			      <% @project.errors.full_messages.each do |message| %>
			        <li><%= message %></li>
			      <% end %>
			    </ul>
  					<% end %>
  						
  					<div class="field form-group">
  						<%= f.label :Name%>
						<%= f.text_field :name , :class => 'form-control',autofocus: true%><br>
  					</div>
					
					<div class="form-group">
	  					<%= f.label :Description %>
						<%= f.text_field :desc , :class => 'form-control'%><br>
					</div>
					
					<div class="form-group">
						<%= f.label :Category %>
						<%= f.text_field :category , :class => 'form-control'%><br>
					</div>

					<div class="form-group">
						<%= f.label :limit %>
						<%= f.number_field :limit , :class => 'form-control'%><br>
					</div>

					<div class="form-group">
          				<%= f.submit "Create" , :class => 'btn btn-block btn-primary'%>
        			</div>
					
					<% end %>
					
			</div>
		</div>
	</div>
</div>

